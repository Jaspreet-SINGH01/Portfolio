{% extends 'base.html.twig' %}

{% block title %} {{ product.name }}  {% endblock %}

{% block body %}


<div class="row">
    <div class="col-md-5">
        <img src="{{ asset('upload/images/products/'~ product.image) }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-fluid">
    </div>
    <div class="col-md-7 my-auto">
        <h3>{{ product.name }}</h3>
        <span class="product-page-price">{{ (product.price / 100)|number_format(2, ',', '.') }} €</span>
        <hr>
        <p>
            {{ product.description|raw }}
        </p>
        <br>
        <a href="{{ path ('add_to_cart', {id: product.id})}}" class="btn btn-success">
            <i class="bi bi-cart-plus">Ajouter au panier</i>
        </a>

        <h2 class="comment-title mt-5">
<span id="comment-count"> {{ product.comments|length }} commentaire(s) </span>
        </h2>
        
    </div>
</div>

{% if app.user %}

<div class="comment-area mb-5">
     {{ form(commentsForm) }} 
</div>

{% endif %}
<br>
<hr>
<br>

<h3>Découvrez les autres articles 👞👠</h3>

    {% for product in products %}
        <div class="row">
            <div class="col-md-3">
                {% include 'products/suggestion.html.twig' %}
            </div>
        </div>
    {% endfor %}
    
{% endblock %} 


