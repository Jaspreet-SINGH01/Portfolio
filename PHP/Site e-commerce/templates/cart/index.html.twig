{% extends 'base.html.twig' %}

{% block title %} Mon panier 🧺 {% endblock %}

{% block body %}
    <h1>Mon panier</h1>
    <br>
    <p style="font-size: 20px; text-align: center">
        Retrouvez l'ensemble des produits que vous avez ajouté à votre panier.

    </p>

    {# <section class="bg-light p-3">
        <h1>Mon panier</h1>
    </section> #}
<br>
<br>
    <table class="table">
        <thead>
            <tr>
                <th>Produit</th>
                <th>Prix</th>
                <th>Quantité</th>
                <th>Total</th>
                <th>Actions</th>
            </tr>
            <tbody>
{# {% for product in dataCart %} #}
{# <tr>
    <td>{{ product.products }}</td>
    <td>{{ product.price }}</td>
    <td>{{ product.quantity }} </td>
    <td>{{ product.quantity * products.price }} </td>
    <td>
        <a href="{{path("add_to_cart", {id: element.products.id} )}}" class="btn btn-success">
            <i class="bi bi-cart-plus"></i>
        </a>
        <a href="{{path("remove_my_cart", {id: element.products.id} )}}" class="btn btn-success"></a> class="btn btn-warning">
            <i class="bi bi-cart-dash"></i>
        </a>
        <a href="{{path("delete_my_cart", {id: element.products.id} )}}" class="btn btn-success"></a> class="btn btn-warning">
            <i class="bi bi-cart-x"></i>
        </a>
    </td>
</tr>
{# {% else %} #}
    <tr>
        <td colspan="5" class="text-center">Votre panier est vide</td>
    </tr> 
    
{# {% endfor %} #}
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3">Total</td>
                    {# <td class="text-end">{{ Total }} €</td> #}
                </tr>
            </tfoot>
        </thead>
    </table>

    {# {% if cart|length > 0 %}
        <table class="table mt-3">
            <thead>
            <tr>
                <th scope="col">Produit</th>
                <th scope="col"></th>
                <th scope="col">Quantité</th>
                <th scope="col">Prix</th>
                <th scope="col">Total</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
            {% set total = null %}
            {% for product in cart %}
            <tr>
                <th>
                    <img src="/uploads/{{ product.product.image }}" alt="{{ product.product.name }}" height="75px"><br/>
                </th>
                <td>
                    {{ product.product.name }}<br/>
                    <small>{{ product.product.description }}</small>
                </td>
                <td>
                    <a href="{{ path('decrease_to_cart', { 'id' : product.product.id }) }}">
                        <img src="{{ asset('assets/img/minus.png') }}" height="12px" alt="Retirer une quantité à mon produit">
                    </a>
                    x {{ product.quantity }}
                    <a href="{{ path('add_to_cart', { 'id' : product.product.id }) }}">
                        <img src="{{ asset('assets/img/plus.png') }}" height="12px" alt="Ajouter un produit">
                    </a>
                </td>
                <td>{{ (product.product.price / 100)|number_format(2, ',', '.') }} €</td>
                <td>{{ ((product.product.price * product.quantity ) / 100)|number_format(2, ',', '.') }} €</td>
                <td>
                    <a href="{{ path('delete_to_cart', { 'id' : product.product.id }) }}">
                        <img src="{{ asset('assets/img/delete.png') }}" height="18px" alt="Supprimer mon produit">
                    </a>
                </td>
            </tr>
                {% set total = total + (product.product.price * product.quantity) %}
            {% endfor %}
            </tbody>
        </table>
        <div class="text-right mb-5">
            <b>Nombre de produit :</b> {{ cart|length }}<br/>
            <b>Total de mon panier :</b> {{ (total / 100)|number_format(2, ',', '.') }} €
            <a href="{{ path('order') }}" class="btn btn-success btn-block mt-3">Valider mon panier</a>
        </div>
    {% else %}
        <hr>
        <p><b>Votre panier est vide.</b></p>
    {% endif %} #}
{% endblock %}
